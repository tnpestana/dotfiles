# Starship Configuration
# Uses default symbols, minimal customization

# Disable username display in all contexts
[username]
show_always = false
disabled = true

# Disable hostname
[hostname]
ssh_only = false
disabled = true

# Disable Google Cloud display (removes email from prompt)
[gcloud]
disabled = true
